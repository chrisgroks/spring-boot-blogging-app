<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="io.spring.infrastructure.mybatis.readservice.UserStatsReadService">

    <select id="countArticlesByUserId" resultType="java.lang.Integer">
        SELECT COUNT(*) FROM articles WHERE user_id = #{userId}
    </select>

    <select id="countFollowersByUserId" resultType="java.lang.Integer">
        SELECT COUNT(*) FROM follows WHERE follow_id = #{userId}
    </select>

    <select id="countFollowingByUserId" resultType="java.lang.Integer">
        SELECT COUNT(*) FROM follows WHERE user_id = #{userId}
    </select>

    <select id="countFavoritesReceivedByUserId" resultType="java.lang.Integer">
        SELECT COUNT(*)
        FROM article_favorites af
        INNER JOIN articles a ON af.article_id = a.id
        WHERE a.user_id = #{userId}
    </select>

</mapper>
